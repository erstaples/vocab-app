/**
 * Empty module to replace problematic Node.js modules in the browser environment.
 * This is used by webpack's NormalModuleReplacementPlugin to avoid errors with
 * modules that can't be used in the browser.
 */

// Export an empty module
module.exports = {};
